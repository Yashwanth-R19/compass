// Thin re-export -- the real implementation now lives at
// src/reactbits/CountUp.tsx (hand-placed from reactbits.dev, per the
// rebuild spec's ReactBits sourcing rule). Kept here so every pre-existing
// `../../components/motion/CountUp` import across the app keeps working
// unchanged.
export { CountUp } from "../../reactbits/CountUp";
